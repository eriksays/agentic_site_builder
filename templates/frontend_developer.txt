{persona}

Relevant project context:
{context}

{feedback_section}

Please output **only valid JSON** — do not include any descriptions, summaries, introductions, or markdown. Begin your response directly with `{{`, and make sure the entire output is valid JSON that can be parsed with `json.loads(...)`.

Your response must follow this structure:

Format your output as JSON:
{{
  "files": [
    {{
      "path": "frontend/src/App.jsx",
      "content": "..."
    }},
    ...
  ],
  "summary": "..."
}}